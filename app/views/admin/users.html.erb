<%= @users.count %>

<ul class="admin-list">
  <% @users.each do |user| %>
    <li class="user-list">
      <div>
        <%= user.name %>
      </div>

      <div class="btn-container">

        <% if user.admin %>
          <%= form_for @admin, url: admin_administration_url(@admin) do |f| %>
            <%= f.hidden_field :id, value: user.id %>
            <%= f.submit "Admin", class: "btn auth-btn", data: {test: "admin-user-#{user.id}"} %>
          <% end %>
        <% else %>
          <%= form_for @admin, url: admin_administration_url(@admin) do |f| %>
            <%= f.hidden_field :id, value: user.id %>
            <%= f.submit "Not Admin", class: "btn deauth-btn", data: {test: "not-admin-user-#{user.id}"} %>
          <% end %>
          <% if user.authorized %>
            <%= form_for @admin, url: admin_authorization_url(@admin) do |f| %>
              <%= f.hidden_field :id, value: user.id %>
              <%= f.submit "Authorized", class: "btn auth-btn", data: {test: "auth-user-#{user.id}"} %>
            <% end %>
          <% else %>
            <%= form_for @admin, url: admin_authorization_url(@admin) do |f| %>
              <%= f.hidden_field :id, value: user.id %>
              <%= f.submit "Unauthorized", class: "btn deauth-btn", data: {test: "deauth-user-#{user.id}"} %>
            <% end %>
          <% end %>
        <% end %>
      </div>
    </li>
  <% end %>
</ul>