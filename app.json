{
  "name":"Heroku Lettuce Meet App",
  "description":"TBA",
  "environments": {
    "test": {
      "addons": ["heroku-postgresql:in-dyno", "heroku-redis:in-dyno"],
      "env": {
        "POSTGRESQL_VERSION": "14",
        "REDIS_VERSION": "6",
        "IS_RUNNING_ON_CI":   "true",
        "BUILDPACK_LOG_FILE": "tmp/lettuceMeet.log"
      },
      "formation": {
        "test": {
          "size":     "performance-l",
          "quantity": 2
        }
      },
      "scripts": {
        "test":       "bundle exec rspec spec"
      },
      "buildpacks": [
        { "url": "heroku/ruby" }
      ]
    }
  }
}
